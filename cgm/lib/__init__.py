"""cgm.lib — vendored trees.

First-party Maya helpers were parked in ``cgm.libOld`` (Spring Cleaning probe).
``from cgm.lib import attributes`` / ``lists`` and the other old first-party
modules fail until those files are shimmed here or callers retarget ``cgm.core``.

Still here: ``zoo`` / ``ml`` / ``bo`` / ``openSource``.
"""
